import { createScaffoldPage, createScaffoldMetadata } from "@/features/scaffold/page-factory";

export default createScaffoldPage("settings");

export const metadata = createScaffoldMetadata("settings");
