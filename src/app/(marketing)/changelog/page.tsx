import { createScaffoldPage, createScaffoldMetadata } from "@/features/scaffold/page-factory";

export default createScaffoldPage("changelog");

export const metadata = createScaffoldMetadata("changelog");
